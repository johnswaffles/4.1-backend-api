{
  "name": "4.1-backend-api",
  "version": "1.1.0",
  "description": "Backend API for Super Intelligent Assistant demo",
  "type": "module",
  "main": "server.js",

  "scripts": {
    "start": "node --max-old-space-size=1024 server.js",
    "dev": "nodemon server.js"
  },

  "engines": {
    "node": ">=22"          // Render’s default right now is v22.x
  },

  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.2",          // <-- fixed
    "openai": "^4.30.0"
  },

  "devDependencies": {
    "nodemon": "^2.0.23"
  }
}